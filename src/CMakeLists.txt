# This file is generated by catkin.
cmake_minimum_required(VERSION 3.0.2)
project(uav_experiment_workspace)

set(CATKIN_TOPLEVEL TRUE)

find_package(catkin QUIET
  NO_POLICY_SCOPE
  PATHS ${CMAKE_PREFIX_PATH}
  NO_DEFAULT_PATH
)

if(NOT catkin_FOUND)
  message(FATAL_ERROR "catkin was not found. Source /opt/ros/noetic/setup.bash first.")
endif()

catkin_workspace()
